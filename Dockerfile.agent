FROM python:3.10-slim
WORKDIR /app

COPY api/api_agent ./api_agent

RUN pip install --no-cache-dir fastapi uvicorn requests langdetect urllib3

EXPOSE 8000
CMD ["uvicorn", "api_agent.main:app", "--host", "0.0.0.0", "--port", "8000"]
